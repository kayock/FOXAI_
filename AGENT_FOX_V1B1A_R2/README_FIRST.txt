Agent Fox Technical Core V1B-1A-R2

Mission: ENG-20260722-062243-69C555

Extract this entire folder directly into Z:\FOXAI.
Resulting plan: Z:\FOXAI\AGENT_FOX_V1B1A_R2\PLAN.json

Preview command:
/engineer workshop preview "Z:\FOXAI\AGENT_FOX_V1B1A_R2\PLAN.json"

IMPORTANT BEFORE APPLY:
Keep the WebUI and Engineering Workshop running. Keep FOXAI Desktop,
ComfyUI, and the llama-server/model engine off. The collector never
closes processes. A remaining workload safely blocks the capture.

This repair replaces only the collector payload. The contract and README
remain byte-for-byte unchanged. Profile evidence is recursively sanitized;
diagnostics report only a JSON field path and category, never the marker.
K: is not accessed.
