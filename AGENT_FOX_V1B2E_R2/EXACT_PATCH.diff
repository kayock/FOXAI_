--- core/director.py.before
+++ core/director.py.after
@@ -66,7 +66,6 @@
     "investigate",
     "investigation",
     "investigation engine",
-    "evidence",
     "ranked evidence",
     "recommendation",
     "engineering assessment",

--- ui/main_window.py.before
+++ ui/main_window.py.after
@@ -2356,7 +2356,8 @@
             if not answer:
                 answer = "[BLANK RESPONSE]\nThe model returned no text. Try restarting the mission or lowering context/max tokens."
 
-            answer = f"[Model: {model_used}]\n\n{answer}"
+            if not answer.lstrip().startswith("[Model:"):
+                answer = f"[Model: {model_used}]\n\n{answer}"
             self.messages.append({"role": "assistant", "content": answer})
             self.add_chat("AGENT FOX", answer)
             self.mission_memory.save()
