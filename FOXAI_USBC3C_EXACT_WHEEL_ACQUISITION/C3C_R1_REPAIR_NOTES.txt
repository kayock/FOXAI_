FOXAI USB C3C R1 — Temporary Filename Validation Repair

REASON FOR R1
The first C3C run correctly downloaded the first exact wheel into a quarantine
file ending in ".whl.partial". The structural validator then passed that
temporary quarantine filename to packaging.parse_wheel_filename(), which
correctly rejects anything not ending in ".whl".

SAFETY RESULT OF THE FAILED RUN
- C3C stopped fail-closed.
- Zero wheels entered STAGING_WHEELHOUSE.
- Zero packages were installed.
- Runtime\ComfyUI\site-packages was not created.
- Desktop, Core, ComfyUI, System, and launchers were unchanged.

EXACT REPAIR
R1 continues to hold new payloads under non-installable ".partial" names.
It now:
1. requires the temporary filename to exactly equal
   NNN__<reviewed-wheel-filename>.partial;
2. parses wheel identity and tags from the immutable reviewed C3B filename;
3. performs all existing byte-size, SHA-256, ZIP, METADATA, WHEEL, and RECORD
   verification before renaming the file into STAGING_WHEELHOUSE.

No acquisition policy, approved URL, package version, hash, size, source host,
installation boundary, or runtime boundary was changed.

RERUN
Extract R1 directly into Z:\FOXAI and allow replacement only inside:
  FOXAI_USBC3C_EXACT_WHEEL_ACQUISITION

Existing ACQUISITION_OUTPUT evidence may remain. The empty staging wheelhouse
from the stopped run may also remain. Run RUN_USB_C3C_ACQUIRE.bat again.
