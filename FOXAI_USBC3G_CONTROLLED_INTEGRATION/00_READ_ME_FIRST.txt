FOXAI USB C3G — CONTROLLED NO-LAUNCH INTEGRATION

Operator approval recorded: “yes please” to proceed from accepted C3F R2.

This is an approved write stage, but it is NOT a launch stage.

C3G applies exactly eight paths:
  - adds the isolated Python activation helper;
  - adds the direct operator-controlled isolated launcher;
  - replaces six exact reviewed live launcher/dependency surfaces.

Before writing, it verifies:
  - the exact accepted C3F evidence run;
  - the exact C3E 39,046-file isolated target;
  - every current live source hash;
  - every sealed candidate hash and Python syntax.

It creates exact backups under APPLY_OUTPUT, stages candidates beside their
live paths, commits with same-volume os.replace, verifies every result, then
re-verifies the full isolated target and protected sentinels.

If a partial apply or post-apply verification fails, C3G restores the six exact
original files and removes only the two exact newly added files. It never
launches ComfyUI during apply or rollback.

Run:
  RUN_USB_C3G_APPLY.bat

Then upload the newest:
  APPLY_OUTPUT\<timestamp>\UPLOAD_THIS_C3G_REVIEW.zip

Do not launch ComfyUI until C3G evidence is reviewed and C3H receives fresh
explicit approval.
