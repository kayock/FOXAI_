FOXAI USB C4D — WebUI Approved Custom Node Profile Apply

This is a controlled WRITE stage but remains NO-LAUNCH.

It replaces exactly three reviewed files and adds one explicit approved-profile BAT.
Safe Normal CPU remains the default. Approved Custom Nodes CPU is explicit-only,
non-persistent, and hash-locks websocket_image_save.py before process creation and
after verified health.

Run RUN_USB_C4D_APPLY.bat and upload the generated UPLOAD_THIS_C4D_REVIEW.zip.
Do not launch ComfyUI until C4D evidence is accepted and C4E is approved.
