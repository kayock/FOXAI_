# Decision 0017 - Feature 001B Runtime Launcher

Runtime startup remains Operator-visible.

KayocktheOS can generate the command, but the Operator launches the model runtime until we have a GUI approval flow.
