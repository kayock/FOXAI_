# v0.2.0 Patch Notes

This is a browser-source patch, not a full replacement release.

Back up your existing files first:

- `index.html`
- `renderer.js`

Then copy the patched files into:

`Z:\KayocktheOS\Shell\KayockBrowser\`

Start KayocktheOS using `Start_KayocktheOS.bat`, then launch the Browser Shell.
