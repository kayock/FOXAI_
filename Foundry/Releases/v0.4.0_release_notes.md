# v0.4.0 Release Notes - Living System Scanner

Adds read-only system scanning to the Core API.

New endpoint:

```text
/api/system
```

The existing `/api/status` endpoint now includes a `system` section.
