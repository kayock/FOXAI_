MICR1A — Mission Image Continuity + Payload Leakage Repair

EXTRACT
-------
Extract the complete folder directly to:

    Z:\FOXAI\MICR1A\

Do not run it from inside the ZIP preview.

BEFORE RUNNING
--------------
Close:

- FOXAI WebUI;
- Chat Engine;
- vision benchmark servers;
- other benchmark servers.

RUN
---
    APPLY.bat

EXACT APPROVAL PHRASE
---------------------
    APPROVE MISSION IMAGE CONTINUITY LEAKAGE REPAIR PHASE 1 APPLY

ALLOWED LIVE SCOPE
------------------
    core\foxai_web.py

Explicitly unchanged:

    core\server.py

No delete operation is included.

TRANSACTIONAL SAFETY
--------------------
- verifies this package manifest;
- verifies the complete approved exact preview;
- verifies the uploaded live exact-preview receipt;
- requires the prior read-only disk scan to report zero persisted image strings;
- verifies the locked live FOXAI baseline and official vision assets;
- runs browser continuity, leakage, cancellation, text-profile, and clear tests;
- runs helper tests proving exactly one image on the current vision turn;
- runs the raw-payload archive/history guard;
- runs Boundary Watch 5/5;
- creates and verifies a backup after explicit approval;
- installs through staged atomic replacement;
- repeats the full suite against the live installed file;
- verifies only core\foxai_web.py changed;
- verifies core\server.py, projector, protected files, and security logs stayed unchanged;
- automatically rolls back on any post-install failure.

SUCCESS
-------
Nothing is considered installed unless the generated receipt says:

    State: applied_verified
    Verified: True

EXPECTED FINAL HASH
-------------------
    core\foxai_web.py
    7fcbddeae22904af7f9aa75e9546e3e28721d455222fbfc42c27c5186ba45180
