# Phase 2B3 R2 Failure Diagnosis

R2 successfully passed:

- the package and live-baseline checks;
- exact reconstruction of the 329 approved wheel files;
- all twelve candidate imports;
- Psutil, Requests, and Casbin functional tests;
- all five Boundary Watch tests.

Those Python imports then generated **138** normal
`__pycache__/*.pyc` files inside the candidate. The candidate was moved into
the live location with **467** total files, while the
post-apply verifier still expected exactly **329** files.

The preserved failed runtime proves:

- approved wheel-owned files: **329**
- generated bytecode files: **138**
- wheel-owned tree digest: `adf6c53a80b8a45b9595e1ddc96b234ecbdb6a4f9529f266ca06d34058d37add`
- expected preview digest: `adf6c53a80b8a45b9595e1ddc96b234ecbdb6a4f9529f266ca06d34058d37add`
- exact wheel-owned tree match: **True**
- all extra files were Python bytecode caches: **True**
- rollback restored both modified files: **True**

R3 disables bytecode creation during every verification subprocess, verifies
the candidate tree again after preflight tests, and verifies the wheel-owned
tree separately from any future legitimate Python cache files.
