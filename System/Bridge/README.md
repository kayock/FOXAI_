# Bridge

The Bridge is the Operator-facing command center for KayocktheOS.

In version 0.0.3, the Bridge is terminal-based.

Future versions may replace or extend it with a graphical interface or Kayock Browser integration.
