--- /dev/null
+++ b/System/EngineeringWorkshop/missions/ENG-20260723-025402-DF9592_V1B3B_SHARED_ROUTER_INTEGRATION_PREFLIGHT/v1b3b_shared_router_integration_preflight.py
@@ -0,0 +1,905 @@
+from __future__ import annotations
+
+import argparse
+import ast
+import hashlib
+import json
+from pathlib import Path
+import re
+from typing import Any, Iterable, Mapping, Optional
+
+
+MISSION_ID = "ENG-20260723-025402-DF9592"
+SCHEMA_PREFIX = "foxai.agent_fox.technical_core.v1b3b"
+EXPECTED_OUTPUT_NAMES = (
+    "V1B3B_PRIMARY_INPUT_INVENTORY.json",
+    "V1B3B_SHARED_ROUTER_FINDINGS.json",
+    "V1B3B_AUTHORIZATION_FINDINGS.json",
+    "V1B3B_PASS_THROUGH_MATRIX.json",
+    "V1B3B_MINIMAL_INTEGRATION_RECOMMENDATION.json",
+    "V1B3B_PREFLIGHT_REPORT.md",
+    "V1B3B_PREFLIGHT_RECEIPT.json",
+)
+
+AUTHORITATIVE_REFERENCE = {
+    "manifest": {
+        "relative_path": "System/AgentFoxTechnicalCore/Reference/TECHNICAL_CORE_REFERENCE_MANIFEST.json",
+        "sha256": "459b1d16af76af323fdd5037af7f044c7accf19db7e40652c3c7a6bb0d883737",
+    },
+    "guide": {
+        "relative_path": "System/AgentFoxTechnicalCore/Reference/TECHNICAL_CORE_REFERENCE_GUIDE.md",
+        "sha256": "d194b5c649e29a06b1b5d2b8438185b35674318d5c5410f1583df59b3782d570",
+    },
+    "reference_build_mission": "ENG-20260723-013919-9712A1",
+    "reference_build_receipt_sha256": "aa999b3b94476ae6f7271b2eba1ed846ad4b842512d5ae279c04c17ffadedb26",
+}
+
+PRIMARY_INPUT_SPECS = (
+    ("reference_manifest", AUTHORITATIVE_REFERENCE["manifest"]["relative_path"], True),
+    ("reference_guide", AUTHORITATIVE_REFERENCE["guide"]["relative_path"], True),
+    ("current_state_broker", "System/AgentFoxTechnicalCore/current_state_request_broker_v1.py", True),
+    ("current_state_contract", "System/AgentFoxTechnicalCore/CURRENT_STATE_REQUEST_BROKER_CONTRACT_V1.json", True),
+    ("current_state_fixtures", "System/AgentFoxTechnicalCore/CURRENT_STATE_REQUEST_FIXTURES_V1.json", True),
+    ("shared_adapter", "System/AgentFoxTechnicalCore/self_knowledge_chat_adapter_v1.py", True),
+    ("desktop_helper", "System/AgentFoxTechnicalCore/desktop_self_knowledge_integration_v1.py", True),
+    ("webui_helper", "System/AgentFoxTechnicalCore/webui_self_knowledge_integration_v1.py", True),
+    ("resource_provider", "System/AgentFoxTechnicalCore/resource_evidence_provider_v1.py", True),
+    ("resource_contract", "System/AgentFoxTechnicalCore/RESOURCE_EVIDENCE_PROVIDER_CONTRACT_V1.json", False),
+    ("resource_fixtures", "System/AgentFoxTechnicalCore/RESOURCE_EVIDENCE_QUERY_FIXTURES_V1.json", False),
+    ("shared_resource_contract", "System/AgentFoxTechnicalCore/SHARED_RESOURCE_PROVIDER_INTEGRATION_CONTRACT_V1.json", False),
+    ("shared_resource_fixtures", "System/AgentFoxTechnicalCore/SHARED_RESOURCE_PROVIDER_INTEGRATION_FIXTURES_V1.json", False),
+    ("desktop_source", "ui/main_window.py", True),
+    ("webui_source", "core/foxai_web.py", True),
+    ("security_containment", "core/security_containment.py", False),
+    ("mission_director", "core/mission_director.py", False),
+    ("engineer_agent", "core/engineer_agent.py", False),
+)
+
+AUTHORIZATION_TERMS = (
+    "authorize_department_route",
+    "operator_approved",
+    "authorization.allowed",
+    "authorization.get(\"allowed\")",
+    "authorization.get('allowed')",
+    "read_only_allowed",
+    "inspect_authorization",
+    "audit_receipt",
+    "direct_mission",
+    "allowed_categories",
+    "allowed_provider_ids",
+    "\"mode\": \"read_only\"",
+    "'mode': 'read_only'",
+)
+
+ROUTING_TERMS = (
+    "route_message(",
+    "startswith(\"/\")",
+    "startswith('/')",
+    "resource_evidence_provider_v1",
+    "_load_resource",
+    "_render_resource",
+    "broker_request",
+    "current_state_request_broker_v1",
+    "_catalog_matches",
+    "_pass_through_result",
+    "ordinary_chat_pass_through",
+    "model_bypass",
+)
+
+PASS_THROUGH_ROWS = (
+    {
+        "broker_status": "slash_command_bypass",
+        "integration_action": "preserve_existing_slash_command_precedence",
+        "intercept": False,
+        "live_execution": False,
+    },
+    {
+        "broker_status": "historical_route_preserved",
+        "integration_action": "continue_to_completed_v1b2_historical_evidence_route",
+        "intercept": False,
+        "live_execution": False,
+    },
+    {
+        "broker_status": "not_applicable",
+        "integration_action": "continue_to_existing_self_knowledge_department_and_model_routes",
+        "intercept": False,
+        "live_execution": False,
+    },
+    {
+        "broker_status": "authorization_required",
+        "integration_action": "return_clearly_labeled_non_execution_answer",
+        "intercept": True,
+        "live_execution": False,
+    },
+    {
+        "broker_status": "provider_unavailable",
+        "integration_action": "return_clearly_labeled_non_execution_answer",
+        "intercept": True,
+        "live_execution": False,
+    },
+    {
+        "broker_status": "unsupported_live_category",
+        "integration_action": "return_clearly_labeled_non_execution_answer",
+        "intercept": True,
+        "live_execution": False,
+    },
+    {
+        "broker_status": "ambiguous_request",
+        "integration_action": "safe_pass_through_without_scan_or_current_measurement_claim",
+        "intercept": False,
+        "live_execution": False,
+    },
+    {
+        "broker_status": "disallowed_intent",
+        "integration_action": "safe_pass_through_without_scan_repair_diagnosis_or_recommendation",
+        "intercept": False,
+        "live_execution": False,
+    },
+    {
+        "broker_status": "provider_scope_mismatch",
+        "integration_action": "return_clearly_labeled_non_execution_answer",
+        "intercept": True,
+        "live_execution": False,
+    },
+    {
+        "broker_status": "provider_error_safe_passthrough",
+        "integration_action": "return_safe_provider_error_without_substituting_historical_evidence",
+        "intercept": True,
+        "live_execution": False,
+    },
+    {
+        "broker_status": "approved_live_result",
+        "integration_action": "future_only_after_separate_provider_and_authorization_mission",
+        "intercept": True,
+        "live_execution": True,
+    },
+)
+
+
+def canonical_bytes(value: Any) -> bytes:
+    return (json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=False) + "\n").encode("utf-8")
+
+
+def pretty_json_bytes(value: Any) -> bytes:
+    return (json.dumps(value, indent=2, sort_keys=True, ensure_ascii=False) + "\n").encode("utf-8")
+
+
+def sha256_bytes(data: bytes) -> str:
+    return hashlib.sha256(data).hexdigest()
+
+
+def line_map(text: str) -> list[str]:
+    return text.splitlines()
+
+
+def bounded_hits(text: str, terms: Iterable[str], *, max_hits: int = 80) -> list[dict[str, Any]]:
+    lines = line_map(text)
+    results: list[dict[str, Any]] = []
+    for number, raw_line in enumerate(lines, 1):
+        lowered = raw_line.casefold()
+        matching = [term for term in terms if term.casefold() in lowered]
+        if matching:
+            results.append(
+                {
+                    "line": number,
+                    "terms": sorted(matching),
+                    "text": raw_line[:500],
+                }
+            )
+            if len(results) >= max_hits:
+                break
+    return results
+
+
+def parse_python(text: str, path: str) -> tuple[Optional[ast.Module], Optional[str]]:
+    try:
+        return ast.parse(text, filename=path), None
+    except Exception as exc:
+        return None, f"{type(exc).__name__}: {exc}"
+
+
+def function_inventory(tree: Optional[ast.Module]) -> list[dict[str, Any]]:
+    if tree is None:
+        return []
+    rows: list[dict[str, Any]] = []
+    for node in ast.walk(tree):
+        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
+            rows.append(
+                {
+                    "name": node.name,
+                    "line": node.lineno,
+                    "end_line": getattr(node, "end_lineno", node.lineno),
+                    "arguments": [arg.arg for arg in node.args.args],
+                    "kind": "async" if isinstance(node, ast.AsyncFunctionDef) else "sync",
+                }
+            )
+    rows.sort(key=lambda row: (row["line"], row["name"]))
+    return rows
+
+
+def call_inventory(tree: Optional[ast.Module], start: int, end: int) -> list[dict[str, Any]]:
+    if tree is None:
+        return []
+    rows: list[dict[str, Any]] = []
+    for node in ast.walk(tree):
+        if not isinstance(node, ast.Call):
+            continue
+        if not (start <= getattr(node, "lineno", -1) <= end):
+            continue
+        func = node.func
+        if isinstance(func, ast.Name):
+            name = func.id
+        elif isinstance(func, ast.Attribute):
+            pieces: list[str] = []
+            current: Any = func
+            while isinstance(current, ast.Attribute):
+                pieces.append(current.attr)
+                current = current.value
+            if isinstance(current, ast.Name):
+                pieces.append(current.id)
+            name = ".".join(reversed(pieces))
+        else:
+            name = type(func).__name__
+        rows.append({"line": node.lineno, "call": name})
+    rows.sort(key=lambda row: (row["line"], row["call"]))
+    return rows
+
+
+def source_excerpt(text: str, start: int, end: int) -> list[dict[str, Any]]:
+    lines = line_map(text)
+    lower = max(1, start)
+    upper = min(len(lines), end)
+    return [{"line": i, "text": lines[i - 1][:600]} for i in range(lower, upper + 1)]
+
+
+def read_primary_inputs(project_root: Path) -> tuple[list[dict[str, Any]], dict[str, str]]:
+    inventory: list[dict[str, Any]] = []
+    texts: dict[str, str] = {}
+    for logical_name, relative_path, required in PRIMARY_INPUT_SPECS:
+        full_path = project_root / Path(relative_path)
+        row: dict[str, Any] = {
+            "logical_name": logical_name,
+            "relative_path": relative_path,
+            "path": str(full_path),
+            "required": required,
+            "exists": full_path.is_file(),
+        }
+        if not full_path.is_file():
+            row.update(
+                {
+                    "size_bytes": None,
+                    "sha256": None,
+                    "utf8_decoded": False,
+                    "python_parsed": None,
+                    "json_parsed": None,
+                    "error": "missing",
+                }
+            )
+            inventory.append(row)
+            continue
+
+        data = full_path.read_bytes()
+        row["size_bytes"] = len(data)
+        row["sha256"] = sha256_bytes(data)
+        try:
+            text = data.decode("utf-8")
+            row["utf8_decoded"] = True
+            texts[logical_name] = text
+        except UnicodeDecodeError as exc:
+            row["utf8_decoded"] = False
+            row["error"] = f"UnicodeDecodeError: {exc}"
+            inventory.append(row)
+            continue
+
+        suffix = full_path.suffix.casefold()
+        if suffix == ".py":
+            tree, error = parse_python(text, str(full_path))
+            row["python_parsed"] = tree is not None
+            row["python_parse_error"] = error
+            row["function_count"] = len(function_inventory(tree))
+            row["json_parsed"] = None
+        elif suffix == ".json":
+            try:
+                json.loads(text)
+                row["json_parsed"] = True
+                row["json_parse_error"] = None
+            except Exception as exc:
+                row["json_parsed"] = False
+                row["json_parse_error"] = f"{type(exc).__name__}: {exc}"
+            row["python_parsed"] = None
+        else:
+            row["python_parsed"] = None
+            row["json_parsed"] = None
+        inventory.append(row)
+    return inventory, texts
+
+
+def verify_reference(inventory: list[dict[str, Any]]) -> dict[str, Any]:
+    by_name = {row["logical_name"]: row for row in inventory}
+    manifest = by_name["reference_manifest"]
+    guide = by_name["reference_guide"]
+    manifest_match = manifest.get("sha256") == AUTHORITATIVE_REFERENCE["manifest"]["sha256"]
+    guide_match = guide.get("sha256") == AUTHORITATIVE_REFERENCE["guide"]["sha256"]
+    return {
+        "manifest_sha256_expected": AUTHORITATIVE_REFERENCE["manifest"]["sha256"],
+        "manifest_sha256_actual": manifest.get("sha256"),
+        "manifest_verified": manifest_match,
+        "guide_sha256_expected": AUTHORITATIVE_REFERENCE["guide"]["sha256"],
+        "guide_sha256_actual": guide.get("sha256"),
+        "guide_verified": guide_match,
+        "reference_build_mission": AUTHORITATIVE_REFERENCE["reference_build_mission"],
+        "reference_build_receipt_sha256": AUTHORITATIVE_REFERENCE["reference_build_receipt_sha256"],
+        "all_verified": manifest_match and guide_match,
+    }
+
+
+def analyze_shared_adapter(text: str) -> dict[str, Any]:
+    tree, parse_error = parse_python(text, "self_knowledge_chat_adapter_v1.py")
+    functions = function_inventory(tree)
+    route = next((row for row in functions if row["name"] == "route_message"), None)
+    if route is None:
+        return {
+            "parsed": tree is not None,
+            "parse_error": parse_error,
+            "route_message_found": False,
+            "function_inventory": functions,
+            "blocker": "route_message_not_found",
+        }
+
+    start = int(route["line"])
+    end = int(route["end_line"])
+    route_lines = line_map(text)[start - 1:end]
+    slash_lines = [
+        start + index
+        for index, line in enumerate(route_lines)
+        if "startswith(\"/\")" in line or "startswith('/')" in line
+    ]
+    broker_lines = [
+        start + index
+        for index, line in enumerate(route_lines)
+        if "broker_request" in line or "current_state_request_broker_v1" in line
+    ]
+    resource_lines = [
+        start + index
+        for index, line in enumerate(route_lines)
+        if "resource" in line.casefold()
+        and (
+            "provider" in line.casefold()
+            or "evidence" in line.casefold()
+            or "render" in line.casefold()
+        )
+    ]
+    catalog_lines = [
+        start + index
+        for index, line in enumerate(route_lines)
+        if "_catalog_matches" in line
+    ]
+    normalize_lines = [
+        start + index
+        for index, line in enumerate(route_lines)
+        if "_normalize_selectors" in line or "_deterministic_request_id" in line
+    ]
+
+    if slash_lines:
+        proposed_after = max(slash_lines)
+    else:
+        proposed_after = start
+    candidates_after_slash = sorted(
+        line for line in (normalize_lines + resource_lines + catalog_lines) if line > proposed_after
+    )
+    proposed_before = candidates_after_slash[0] if candidates_after_slash else min(end, proposed_after + 1)
+
+    return {
+        "parsed": tree is not None,
+        "parse_error": parse_error,
+        "route_message_found": True,
+        "route_message": route,
+        "slash_command_lines": slash_lines,
+        "existing_current_state_broker_lines": broker_lines,
+        "historical_resource_or_render_lines": resource_lines,
+        "catalog_match_lines": catalog_lines,
+        "normalization_and_request_id_lines": normalize_lines,
+        "proposed_integration_anchor": {
+            "after_line": proposed_after,
+            "before_line": proposed_before,
+            "rule": (
+                "After exact slash-command bypass and required request normalization, "
+                "before historical resource-evidence loading/catalog matching."
+            ),
+        },
+        "route_calls_in_order": call_inventory(tree, start, end),
+        "route_excerpt": source_excerpt(
+            text,
+            max(start, proposed_after - 4),
+            min(end, proposed_before + 8),
+        ),
+        "routing_term_hits": bounded_hits(text, ROUTING_TERMS),
+        "function_inventory": functions,
+    }
+
+
+def analyze_surface(logical_name: str, text: str) -> dict[str, Any]:
+    tree, parse_error = parse_python(text, logical_name)
+    functions = function_inventory(tree)
+    route_hits = bounded_hits(
+        text,
+        (
+            "route_message(",
+            "self_knowledge",
+            "director",
+            "department",
+            "model",
+            "slash",
+            "authorization",
+        ),
+        max_hits=120,
+    )
+    return {
+        "logical_name": logical_name,
+        "parsed": tree is not None,
+        "parse_error": parse_error,
+        "route_message_call_lines": [
+            row["line"] for row in route_hits if "route_message(" in row["text"]
+        ],
+        "bounded_route_hits": route_hits,
+        "function_count": len(functions),
+    }
+
+
+def analyze_authorization(texts: Mapping[str, str]) -> dict[str, Any]:
+    file_rows: list[dict[str, Any]] = []
+    exact_broker_envelope_hits: list[dict[str, Any]] = []
+    for logical_name in (
+        "shared_adapter",
+        "desktop_helper",
+        "webui_helper",
+        "desktop_source",
+        "webui_source",
+        "security_containment",
+        "mission_director",
+        "engineer_agent",
+    ):
+        text = texts.get(logical_name)
+        if text is None:
+            continue
+        hits = bounded_hits(text, AUTHORIZATION_TERMS, max_hits=120)
+        row = {
+            "logical_name": logical_name,
+            "hit_count": len(hits),
+            "hits": hits,
+        }
+        file_rows.append(row)
+        for hit in hits:
+            hit_text = hit["text"].casefold()
+            if (
+                "allowed_categories" in hit_text
+                or "allowed_provider_ids" in hit_text
+                or "\"mode\": \"read_only\"" in hit_text
+                or "'mode': 'read_only'" in hit_text
+            ):
+                exact_broker_envelope_hits.append(
+                    {
+                        "logical_name": logical_name,
+                        "line": hit["line"],
+                        "text": hit["text"],
+                    }
+                )
+
+    adapter_text = texts.get("shared_adapter", "")
+    adapter_tree, _ = parse_python(adapter_text, "shared_adapter")
+    route_function = next(
+        (
+            row
+            for row in function_inventory(adapter_tree)
+            if row["name"] == "route_message"
+        ),
+        None,
+    )
+    route_arguments = route_function.get("arguments", []) if route_function else []
+    route_accepts_explicit_authorization = any(
+        "authoriz" in argument.casefold() for argument in route_arguments
+    )
+
+    suitable = bool(exact_broker_envelope_hits and route_accepts_explicit_authorization)
+    if suitable:
+        conclusion = "suitable_authorization_source_proven"
+    else:
+        conclusion = "no_suitable_current_state_authorization_source_proven"
+
+    return {
+        "conclusion": conclusion,
+        "suitable_authorization_source_proven": suitable,
+        "route_message_arguments": route_arguments,
+        "route_message_accepts_explicit_authorization": route_accepts_explicit_authorization,
+        "exact_broker_authorization_envelope_hits_outside_broker": exact_broker_envelope_hits,
+        "bounded_file_findings": file_rows,
+        "important_distinction": (
+            "Engineering/department authorization tokens do not by themselves prove a "
+            "request-scoped current-state authorization envelope containing read_only mode, "
+            "allowed_categories, and allowed_provider_ids."
+        ),
+    }
+
+
+def parse_contract(text: Optional[str]) -> dict[str, Any]:
+    if not text:
+        return {"available": False}
+    try:
+        value = json.loads(text)
+    except Exception as exc:
+        return {"available": True, "parsed": False, "error": f"{type(exc).__name__}: {exc}"}
+    return {
+        "available": True,
+        "parsed": True,
+        "schema": value.get("schema"),
+        "mission_id": value.get("mission_id"),
+        "scope": value.get("scope"),
+        "statuses": value.get("statuses"),
+        "authorization_contract": value.get("authorization_contract"),
+        "provider_contract": value.get("provider_contract"),
+    }
+
+
+def build_outputs(project_root: Path) -> dict[str, bytes]:
+    inventory_rows, texts = read_primary_inputs(project_root)
+    missing_required = [
+        row["relative_path"]
+        for row in inventory_rows
+        if row["required"] and not row["exists"]
+    ]
+    reference = verify_reference(inventory_rows)
+
+    shared_findings = analyze_shared_adapter(texts.get("shared_adapter", ""))
+    shared_findings.update(
+        {
+            "schema": f"{SCHEMA_PREFIX}.shared_router_findings.v1",
+            "mission_id": MISSION_ID,
+            "shared_adapter_path": str(
+                project_root / "System/AgentFoxTechnicalCore/self_knowledge_chat_adapter_v1.py"
+            ),
+            "shared_adapter_sha256": next(
+                (
+                    row.get("sha256")
+                    for row in inventory_rows
+                    if row["logical_name"] == "shared_adapter"
+                ),
+                None,
+            ),
+            "desktop_helper": analyze_surface(
+                "desktop_helper", texts.get("desktop_helper", "")
+            ),
+            "webui_helper": analyze_surface(
+                "webui_helper", texts.get("webui_helper", "")
+            ),
+            "desktop_source": analyze_surface(
+                "desktop_source", texts.get("desktop_source", "")
+            ),
+            "webui_source": analyze_surface(
+                "webui_source", texts.get("webui_source", "")
+            ),
+            "reference_verified": reference["all_verified"],
+        }
+    )
+
+    authorization = analyze_authorization(texts)
+    authorization.update(
+        {
+            "schema": f"{SCHEMA_PREFIX}.authorization_findings.v1",
+            "mission_id": MISSION_ID,
+            "reference_verified": reference["all_verified"],
+        }
+    )
+
+    contract_summary = parse_contract(texts.get("current_state_contract"))
+    pass_through = {
+        "schema": f"{SCHEMA_PREFIX}.pass_through_matrix.v1",
+        "mission_id": MISSION_ID,
+        "current_state_contract": contract_summary,
+        "rows": list(PASS_THROUGH_ROWS),
+        "historical_evidence_must_never_be_reused_as_current": True,
+        "ordinary_chat_must_not_be_intercepted": True,
+        "slash_commands_must_not_load_broker_or_providers": True,
+    }
+
+    existing_broker_calls = shared_findings.get("existing_current_state_broker_lines", [])
+    blockers: list[str] = []
+    if missing_required:
+        blockers.append("required_primary_inputs_missing")
+    if not reference["all_verified"]:
+        blockers.append("authoritative_reference_hash_mismatch")
+    if not shared_findings.get("route_message_found"):
+        blockers.append("canonical_route_message_not_found")
+    if existing_broker_calls:
+        blockers.append("current_state_broker_already_present_in_shared_adapter")
+
+    authorization_proven = authorization["suitable_authorization_source_proven"]
+    recommendation_status = (
+        "preflight_complete_ready_for_classification_only_integration"
+        if not blockers
+        else "preflight_blocked_for_exact_review"
+    )
+    recommendation = {
+        "schema": f"{SCHEMA_PREFIX}.minimal_integration_recommendation.v1",
+        "mission_id": MISSION_ID,
+        "status": recommendation_status,
+        "blockers": blockers,
+        "recommended_next_stage": (
+            "V1B-3C classification-only shared-router integration"
+            if not blockers
+            else "Resolve preflight blockers before integration"
+        ),
+        "authorization_source_proven": authorization_proven,
+        "live_provider_registry_available": False,
+        "integration_scope": {
+            "modify_only_shared_adapter": True,
+            "desktop_source_change_required": False,
+            "webui_source_change_required": False,
+            "desktop_helper_change_expected": False,
+            "webui_helper_change_expected": False,
+            "historical_resource_provider_change_required": False,
+            "current_state_broker_change_required": False,
+        },
+        "exact_routing_anchor": shared_findings.get("proposed_integration_anchor"),
+        "classification_only_behavior": {
+            "broker_authorization": None,
+            "provider_registry": {},
+            "can_execute_live_provider": False,
+            "may_return_authorization_required": True,
+            "may_return_provider_unavailable": True,
+            "historical_route_preserved": True,
+            "ordinary_chat_pass_through": True,
+            "diagnosis_or_repair_not_executed": True,
+        },
+        "future_separate_missions_required_for": [
+            "request-scoped approved read-only authorization source",
+            "each bounded approved live-state provider",
+            "approved_live_result user-facing rendering",
+            "live Desktop and WebUI verification",
+        ],
+        "do_not_change_in_next_classification_only_stage": [
+            "core/foxai_web.py",
+            "ui/main_window.py",
+            "System/AgentFoxTechnicalCore/desktop_self_knowledge_integration_v1.py",
+            "System/AgentFoxTechnicalCore/webui_self_knowledge_integration_v1.py",
+            "System/AgentFoxTechnicalCore/resource_evidence_provider_v1.py",
+            "System/AgentFoxTechnicalCore/current_state_request_broker_v1.py",
+        ],
+        "non_goals": [
+            "live-state provider implementation",
+            "automatic scans",
+            "background monitoring",
+            "process inspection",
+            "service inspection",
+            "listener or port inspection",
+            "memory or storage measurement",
+            "startup or registry inspection",
+            "network access",
+            "diagnosis",
+            "repair",
+            "optimization",
+            "recommendations",
+            "model calls",
+            "GUI launches",
+            "K drive access",
+        ],
+    }
+
+    inventory = {
+        "schema": f"{SCHEMA_PREFIX}.primary_input_inventory.v1",
+        "mission_id": MISSION_ID,
+        "authoritative_reference": reference,
+        "bounded_input_count": len(inventory_rows),
+        "required_input_count": sum(1 for row in inventory_rows if row["required"]),
+        "missing_required_inputs": missing_required,
+        "inputs": inventory_rows,
+        "filesystem_search_performed": False,
+        "fixed_file_list_only": True,
+    }
+
+    report_lines = [
+        "# Agent Fox Technical Core V1B-3B",
+        "",
+        "## Shared-Router Integration Preflight",
+        "",
+        f"- Mission: `{MISSION_ID}`",
+        f"- Status: `{recommendation_status}`",
+        f"- Authoritative reference verified: `{reference['all_verified']}`",
+        f"- Required inputs missing: `{len(missing_required)}`",
+        f"- Canonical `route_message()` found: `{shared_findings.get('route_message_found', False)}`",
+        f"- Existing current-state broker call in shared adapter: `{bool(existing_broker_calls)}`",
+        f"- Suitable request-scoped current-state authorization source proven: `{authorization_proven}`",
+        "",
+        "## Minimal recommendation",
+        "",
+        (
+            "Proceed only with a classification-only shared-adapter integration stage. "
+            "Invoke the installed broker with no authorization and an empty provider registry, "
+            "so no live execution is possible. Preserve slash-command precedence, the completed "
+            "V1B-2 historical route, ordinary chat pass-through, Desktop/WebUI helpers, and all "
+            "product source files outside the shared adapter."
+            if not blockers
+            else "Do not integrate until the listed blockers are resolved."
+        ),
+        "",
+        "## Exact proposed routing anchor",
+        "",
+        "```json",
+        json.dumps(shared_findings.get("proposed_integration_anchor"), indent=2, sort_keys=True),
+        "```",
+        "",
+        "## Blockers",
+        "",
+    ]
+    report_lines.extend([f"- `{item}`" for item in blockers] or ["- None"])
+    report_lines.extend(
+        [
+            "",
+            "## Safety",
+            "",
+            "- Read fixed files only.",
+            "- No filesystem search.",
+            "- No live resource inspection.",
+            "- No process, service, listener, port, memory, storage, startup, or registry inspection.",
+            "- No network access, model calls, GUI launches, installs, repairs, or K: access.",
+            "- No existing Technical Core, Desktop, WebUI, or provider source was modified.",
+            "",
+        ]
+    )
+    report_bytes = ("\n".join(report_lines)).encode("utf-8")
+
+    non_receipt: dict[str, bytes] = {
+        "V1B3B_PRIMARY_INPUT_INVENTORY.json": pretty_json_bytes(inventory),
+        "V1B3B_SHARED_ROUTER_FINDINGS.json": pretty_json_bytes(shared_findings),
+        "V1B3B_AUTHORIZATION_FINDINGS.json": pretty_json_bytes(authorization),
+        "V1B3B_PASS_THROUGH_MATRIX.json": pretty_json_bytes(pass_through),
+        "V1B3B_MINIMAL_INTEGRATION_RECOMMENDATION.json": pretty_json_bytes(recommendation),
+        "V1B3B_PREFLIGHT_REPORT.md": report_bytes,
+    }
+    output_rows = [
+        {
+            "name": name,
+            "size_bytes": len(data),
+            "sha256": sha256_bytes(data),
+        }
+        for name, data in sorted(non_receipt.items())
+    ]
+    receipt = {
+        "schema": f"{SCHEMA_PREFIX}.preflight_receipt.v1",
+        "mission_id": MISSION_ID,
+        "status": recommendation_status,
+        "authoritative_reference_verified": reference["all_verified"],
+        "required_inputs_missing": missing_required,
+        "exact_output_count_including_receipt": len(EXPECTED_OUTPUT_NAMES),
+        "outputs_excluding_receipt": output_rows,
+        "existing_live_source_files_modified": 0,
+        "preflight_runner_file_added_by_plan": 1,
+        "readme_file_added_by_plan": 1,
+        "live_state_providers_invoked": 0,
+        "live_scans": 0,
+        "process_inspection": False,
+        "service_inspection": False,
+        "listener_inspection": False,
+        "port_inspection": False,
+        "memory_measurement": False,
+        "storage_measurement": False,
+        "startup_inspection": False,
+        "registry_inspection": False,
+        "network_used": False,
+        "model_calls": 0,
+        "guis_launched": 0,
+        "packages_installed": False,
+        "diagnosis_performed": False,
+        "repair_performed": False,
+        "optimization_performed": False,
+        "recommendations_about_system_performance": False,
+        "k_access": False,
+        "filesystem_search_performed": False,
+        "fixed_file_list_only": True,
+        "historical_evidence_used_as_current": False,
+        "authorization_source_proven": authorization_proven,
+        "live_provider_registry_available": False,
+        "shared_adapter_modified": False,
+        "desktop_modified": False,
+        "webui_modified": False,
+        "resource_provider_modified": False,
+    }
+    outputs = dict(non_receipt)
+    outputs["V1B3B_PREFLIGHT_RECEIPT.json"] = pretty_json_bytes(receipt)
+    return outputs
+
+
+def write_outputs(project_root: Path, output_dir: Path) -> dict[str, Any]:
+    outputs = build_outputs(project_root)
+    first = {name: data for name, data in outputs.items()}
+    second = build_outputs(project_root)
+    if first != second:
+        raise AssertionError("deterministic_rebuild_mismatch")
+
+    output_dir.mkdir(parents=True, exist_ok=True)
+    expected = set(EXPECTED_OUTPUT_NAMES)
+    for name, data in outputs.items():
+        if name not in expected:
+            raise AssertionError(f"unexpected_output_name:{name}")
+        target = output_dir / name
+        target.write_bytes(data)
+
+    actual = {path.name for path in output_dir.iterdir() if path.is_file()}
+    allowed_non_outputs = {
+        "v1b3b_shared_router_integration_preflight.py",
+        "README_V1B3B.md",
+    }
+    unexpected = sorted(actual - expected - allowed_non_outputs)
+    if unexpected:
+        raise AssertionError(f"unexpected_files_in_output_dir:{unexpected}")
+
+    return {
+        "mission_id": MISSION_ID,
+        "output_dir": str(output_dir),
+        "output_count": len(outputs),
+        "deterministic": True,
+        "status": json.loads(outputs["V1B3B_PREFLIGHT_RECEIPT.json"])["status"],
+    }
+
+
+def self_test() -> dict[str, Any]:
+    synthetic = """
+def route_message(message, surface, request_id=None, selectors=None):
+    normalized_message = str(message or "")
+    if normalized_message.lstrip().startswith("/"):
+        return pass_through()
+    normalized_selectors = normalize(selectors)
+    rid = request_id or make_id(normalized_message)
+    bridge = load_bridge()
+    candidates = _catalog_matches(normalized_message, normalized_selectors, bridge)
+    return candidates
+"""
+    findings = analyze_shared_adapter(synthetic)
+    assert findings["route_message_found"]
+    assert findings["slash_command_lines"]
+    assert findings["catalog_match_lines"]
+    anchor = findings["proposed_integration_anchor"]
+    assert anchor["after_line"] < anchor["before_line"]
+
+    auth = analyze_authorization(
+        {
+            "shared_adapter": synthetic,
+            "webui_source": (
+                "authorization = direct_mission(text)\n"
+                "read_only_allowed = True\n"
+                "operator_approved = True\n"
+            ),
+        }
+    )
+    assert auth["suitable_authorization_source_proven"] is False
+    assert auth["conclusion"] == "no_suitable_current_state_authorization_source_proven"
+    return {
+        "schema": f"{SCHEMA_PREFIX}.self_test.v1",
+        "mission_id": MISSION_ID,
+        "passed": True,
+        "tests": 8,
+    }
+
+
+def main(argv: Optional[list[str]] = None) -> int:
+    parser = argparse.ArgumentParser(description="V1B-3B shared-router integration preflight")
+    parser.add_argument("--project-root")
+    parser.add_argument("--output-dir")
+    parser.add_argument("--self-test", action="store_true")
+    args = parser.parse_args(argv)
+
+    if args.self_test:
+        print(json.dumps(self_test(), indent=2, sort_keys=True))
+        return 0
+
+    if not args.project_root or not args.output_dir:
+        parser.error("--project-root and --output-dir are required unless --self-test is used")
+
+    project_root = Path(args.project_root)
+    output_dir = Path(args.output_dir)
+    result = write_outputs(project_root, output_dir)
+    print(json.dumps(result, indent=2, sort_keys=True))
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
--- /dev/null
+++ b/System/EngineeringWorkshop/missions/ENG-20260723-025402-DF9592_V1B3B_SHARED_ROUTER_INTEGRATION_PREFLIGHT/README_V1B3B.md
@@ -0,0 +1,21 @@
+# Agent Fox Technical Core V1B-3B
+
+Mission: `ENG-20260723-025402-DF9592`
+
+This is a static, bounded shared-router integration preflight.
+
+The plan adds only the preflight runner and this README inside:
+
+`Z:\FOXAI\System\EngineeringWorkshop\missions\ENG-20260723-025402-DF9592_V1B3B_SHARED_ROUTER_INTEGRATION_PREFLIGHT`
+
+The runner reads a fixed list of Technical Core, Desktop, WebUI, and authorization
+source files. It does not walk or search the filesystem. It generates seven
+deterministic preflight outputs in the same mission directory.
+
+It performs no live-state measurement, process/service/listener/port inspection,
+memory/storage/startup/registry inspection, network access, model call, GUI launch,
+installation, diagnosis, repair, optimization, recommendation about system
+performance, or K: access.
+
+It does not modify the shared adapter, Desktop, WebUI, helpers, V1B-3A broker,
+V1B-2 provider, or historical evidence route.
