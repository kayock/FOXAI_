FOXAI V2C.1.1 — COMPACT PACKAGE

This is the identical Engineering Workshop plan repacked with short package paths
to avoid Windows error 0x80010135: Path too long.

Extract the V2C11 folder directly into:

    Z:\FOXAI\V2C11

Then preview:

    /engineer workshop preview "Z:\FOXAI\V2C11\plan.json"

Do not use Skip on the failed long-path extraction. Cancel it and use this package.

The plan content and plan SHA-256 are unchanged.
