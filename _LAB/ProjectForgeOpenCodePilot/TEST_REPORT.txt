PROJECT FORGE — OPENCODE PILOT TEST REPORT
=========================================

Package version: 0.1.0

VERIFIED IN THE BUILD ENVIRONMENT
- Python syntax compilation passed.
- Browser control panel started and /api/status returned valid JSON.
- Simulated end-to-end agent run passed:
  * broken baseline tests failed;
  * mock OpenCode edited calculator.py;
  * independent tests passed;
  * protected test file remained unchanged;
  * Git commit was created;
  * rollback restored the broken baseline and reproduced the failure;
  * restore returned to the passing commit;
  * no simulated host-profile writes were detected.

REQUIRES ERIC'S WINDOWS LIVE TEST
- Downloading the current official OpenCode Windows x64 release.
- Windows Authenticode verification of opencode.exe.
- First-run acquisition of OpenCode's OpenAI-compatible provider package.
- Real Qwen3-Coder connection on 127.0.0.1:8080.
- Real OpenCode repair behavior and Windows shell compatibility.
- Host-profile write audit on Eric's PC.

The package is intentionally labeled READY_FOR_LIVE_TEST rather than fully accepted.
